apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crime-court-message-processor-service.fullname" . }}-test-connection"
  labels:
    {{- include "crime-court-message-processor-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crime-court-message-processor-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
